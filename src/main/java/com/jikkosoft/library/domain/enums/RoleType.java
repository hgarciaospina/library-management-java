package com.jikkosoft.library.domain.enums;

/**
 * Enum representing the different types of user roles in the system.
 * Used by Role entities and for permission checks.
 */
public enum RoleType {
    NORMAL_USER,
    ADMIN,
    SUPER_USER
}
